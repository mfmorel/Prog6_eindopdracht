@model IEnumerable<Domain.Hotelkamer_type>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Kosten)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Kosten)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Type }) |
            @Html.ActionLink("Details", "Details", new { id=item.Type }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Type })
        </td>
    </tr>
}

</table>
